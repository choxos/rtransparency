# Identify mentions of funds in acknowledgements

Returns the index with the elements of interest. More generic than \_1.

Returns the index with the elements of interest. More generic than \_1.

## Usage

``` r
get_fund_acknow(article)

get_fund_acknow(article)
```

## Arguments

- article:

  A List with paragraphs of interest.

## Value

The index of the paragraph of interest.

The index of the paragraph of interest.
