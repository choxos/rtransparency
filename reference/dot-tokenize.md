# Tokenize NLM XML files

The code for this was modified from the oddpub package.

## Usage

``` r
.tokenize(article)
```

## Arguments

- article:

  The article as a string

## Value

List with one element per document. Each document is split into its
sentences and saved as a vector of strings.
