# Create a regex for titles

Returns words designed to identify titles.

## Usage

``` r
.title_strict(x, within_text = F)
```

## Arguments

- x:

  A vector of strings.

- within_text:

  Boolean defines whether a regex typical to a title found within text
  should be created or not.

## Value

A vector of strings with a suffix attached.
