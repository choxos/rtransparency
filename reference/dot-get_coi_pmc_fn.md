# Identify elements with COI attributes

Extract elements with COI attributes from NLM XLMs.

## Usage

``` r
.get_coi_pmc_fn(article_xml)
```

## Arguments

- article_xml:

  The text as an xml_document.

## Value

The title and its related text as a string.
