# Extract PMIDs and DOIs

Reads a PMC XML as an xml_document and extracts the DOI, PMID and PMCID
as a list.

## Usage

``` r
.get_ids(article_xml)
```

## Arguments

- article_xml:

  The article as an xml_document.

## Value

A list of PubMed IDs
