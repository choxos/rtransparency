# Get common phrases

Returns the index with the elements of interest.

Returns the index with the elements of interest.

## Usage

``` r
get_common_2(article)

get_common_2(article)
```

## Arguments

- article:

  A List with paragraphs of interest.

## Value

The index of the paragraph of interest.

The index of the paragraph of interest.
